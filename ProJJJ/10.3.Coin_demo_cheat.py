import random

# Класс Coin имитирует монету, которую
# можно подбрасывать. Несколько раз.

class Coin:

    # Метод _init_ инициализирует
    # атрибут данных sideup значением 'Орел'.

    def __init__(self):
         self.sideup = 'Орел'

    # Метод toss генерирует случайное
    # в диапазоне от 0 до 1. Если это число
    # равно 0, то sideup получает значение 'Орел'.
    # В противном случае sideup получает значение 'Решка'.

    def toss(self):
        if random.randint(0, 1) == 0:
            self.sideup = 'Орел'
        else:
            self.sideup = 'Решка'

    # Метод get_sideup возвращает значение,
    # на которые ссылается sideup.

    def get_sideup(self):
        return  self.sideup

# Главная функция.
def main():
    # Создаем объект на основе класса Coin.
    my_coin = Coin()

    # Показать обращённую вверх сторону монеты.
    print('Эта сторона обращена вврех: ', my_coin.get_sideup())

    # Подбросить монету.
    print('Подбрасываем монету...')
    my_coin.toss()

    # В этом объекте
    # я собираюсь на прямую поменять
    # значение атрибута sideup на 'Орел'.
    my_coin.sideup = 'Орел'

    # Показать обращённую вверх сторону монеты.
    print('Эта сторона обращена вверх: ', my_coin.get_sideup())

# Вызвать главную функцию.
main()
